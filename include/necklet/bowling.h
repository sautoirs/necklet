#ifndef BOWLING_H
#define BOWLING_H

#include <stddef.h>
#include <stdint.h>

/// Return the total score of an American Ten-Pin Bowling game.
uint32_t count_score(const char *input, size_t size);

#endif

